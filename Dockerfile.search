# 검색 API 전용 Dockerfile
FROM python:3.12-slim

WORKDIR /app

# 의존성 먼저 복사 (캐시 효율)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 소스 복사
COPY main.py app.py .env ./

EXPOSE 8000

CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
